<!--<div class="text-center">-->
<!--  <% if user_signed_in? %>-->
<!--    <%= form_tag(search_path, method: :get) do %>-->
<!--      <%= text_field_tag 'content' %>-->
<!--      <%= select_tag 'model', options_for_select({ User => "user", Book => "book"}) %>-->
<!--      <%= select_tag 'method', options_for_select({ "完全一致" => "perfect", "部分一致" => "partial", "前方一致" => "forward", "後方一致" => "backward"}) %>-->
<!--      <%= submit_tag '検索' %>-->
<!--    <% end %>-->
<!--  <% end %>-->
<!--</div>-->

<div class="text-center">
  <% if user_signed_in? %>
    <%= form_with url: search_path, method: :get, local: true do |f| %>
      <%= f.text_field 'content' %>
      <%= f.select 'model', options_for_select({ User => "user", Book => "book"}) %>
      <%= f.select 'method', options_for_select({ "完全一致" => "perfect", "部分一致" => "partial", "前方一致" => "forward", "後方一致" => "backward"}) %>
      <%= f.submit '検索' %>
    <% end %>
  <% end %>
</div>